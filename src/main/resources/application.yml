spring:
  application:
    name: KuimaKulak
  datasource:
    url: jdbc:postgresql://localhost:5434/userdb
    username: prod_admin
    password: pass123!
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 492537232969-g2cdddrm2todlat6at2a7acp2nge5gas.apps.googleusercontent.com
            client-secret: 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
            scope: openid, profile, email
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            client-name: Google
          apple:
            client-id: APPLE_CLIENT_ID
            scope: name, email
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            authorization-grant-type: authorization_code
            client-authentication-method: client_secret_post
            client-name: Apple
        provider:
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
            token-uri: https://oauth2.googleapis.com/token
            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo
            user-name-attribute: sub
            jwk-set-uri: https://www.googleapis.com/oauth2/v3/certs
          apple:
            authorization-uri: https://appleid.apple.com/auth/authorize
            token-uri: https://appleid.apple.com/auth/token
            jwk-set-uri: https://appleid.apple.com/auth/keys
            user-name-attribute: sub

app:
  jwt:
    secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
    expiration-ms: 86400000
    refresh-token:
      expiration-ms: 604800000

apple:
  team-id: APPLE_TEAM_ID
  key-id: APPLE_KEY_ID
  private-key-path: classpath:apple-private-key.p8
  client-id: APPLE_SERVICES_ID

server:
  port: 8080

logging:
  level:
    org.springframework.security.oauth2: DEBUG
